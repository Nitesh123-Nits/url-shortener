<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page- URL Shortener</title>
</head>
<style>
    body {
        font-family: sans-serif;
    }

    input {
        padding: 1rem;
        margin: 1rem;
    }



    button {
        padding: 1rem;
        color: darkslategray;
        border: 1px solid darkblue;
        background-color: blanchedalmond;
    }
</style>

<body>
    <h1> URL Shortener</h1>
    <% if(locals.id){ %>
        <p> URP Generated: http://localhost:8001/url/<%= id %>
                <a href="http://localhost:8001/url/<%= id %>"> Click here</a>
        </p>

        <% } %>

            <div class="form-container">
                <form method="POST" action="/url">
                    <label>Enter your url</label>
                    <input type="text" name="url" placeholder="https://example.com" />
                    <button type="submit">Generate URL</button>
                </form>
            </div>
            <div>
                <% if (locals.urls) { %>
                    <table>
                        <thead>
                            <th>S. No</th>
                            <th>ShortID</th>
                            <th>Redirect</th>
                            <th>Clicks</th>
                        </thead>
                        <tbody>

                            <% urls.forEach((url,ind)=> { %>
                                <tr>
                                    <td>
                                        <%= ind+1 %>
                                    </td>
                                    <td>
                                        <%= url.shortId %>
                                    </td>
                                    <td>
                                        <%= url.redirectURL%>
                                    </td>
                                    <td>
                                        <%= url.visitHistory.length %>
                                    </td>
                                </tr>
                                <% }) %>

                        </tbody>
                    </table>
                    <% } %>
            </div>
</body>

</html>